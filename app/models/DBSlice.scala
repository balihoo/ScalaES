package models

import play.api.db.slick.DB
import play.api.Play.current

/*  attempting to do cake stuff...but gave up


trait DBSlice { this:BrandSlice =>
  val db = DB("readmodel")
  override val profile = db.driver
}

 */
